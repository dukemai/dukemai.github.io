import React from 'react';
import PropTypes from 'prop-types';

import Box from '../Box';

const propTypes = {};
const defaultProps = {};
const Bio = ({}) => (
  <Box
    title="Biography"
    content="a challenge lover, positive thinker and javascript lover"
  />
);
Bio.propTypes = propTypes;
Bio.defaultProps = defaultProps;
export default Bio;
