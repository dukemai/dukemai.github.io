import React from 'react';
import PropTypes from 'prop-types';

import Box from '../Box';

const propTypes = {};
const defaultProps = {};
const Languages = ({}) => (
  <Box
    title="Languages"
    content="English, Swedish, Vietnamese"
  />
);
Languages.propTypes = propTypes;
Languages.defaultProps = defaultProps;
export default Languages;
